{
    // Put a relative path to the workspace directory to begin the checking.
    "start_path": "Init.lua",
    "preload": {
        // This indicates that we are using Lua 5.1 and all built-in libraries of it.
        "open": ["lua51"],
        "require": [".vscode/typing"]
    }
}
